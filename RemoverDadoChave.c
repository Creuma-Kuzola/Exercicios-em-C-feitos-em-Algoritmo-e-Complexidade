#include <stdio.h>
#include <stdlib.h>

int removerDadoChave(TVet *vet, TItem item, int pos);

int removerDadoChave(TVet *vet, TItem item, int pos)
{
		if(estaVazio(vet))
					printf("Erro: Impossível remover, o vector está vazio");
		else if(!estaContido(0,vet.nElem-1,pos))			
												printf("Erro: Posição inválida");
							else
							{
										for(int )
									
							
							}					
}

int main()
{



return 0;
}
